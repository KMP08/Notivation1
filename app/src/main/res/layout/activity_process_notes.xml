<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TextView to display the processing method -->
    <TextView
        android:id="@+id/tvMethod"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Processing Method: "
        android:textSize="18sp"
        android:layout_marginTop="20dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <!-- TextView to display the processed notes -->
    <TextView
        android:id="@+id/tvProcessedNotes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Processed Notes will appear here."
        android:textSize="16sp"
        android:layout_below="@id/tvMethod"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true" />

    <!-- Button to save processed notes -->
    <Button
        android:id="@+id/btnSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Notes"
        android:layout_below="@id/tvProcessedNotes"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
